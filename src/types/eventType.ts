export interface EventType {
    id: number
    description: string
    startDate: Date
    endDate: Date
    descriptionText: string
    schedule: string
    location: string
    eventDateTime: string
    observations: string
    nisRequired: boolean
}
